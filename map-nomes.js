const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesPadronizados = nomes.map(nome  =>  nome.toUpperCase()); //Retorna os nomes com letras maiusculas

console.log(nomesPadronizados);